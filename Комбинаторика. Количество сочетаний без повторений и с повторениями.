# Комбинаторика. Количество сочетаний без повторений и с повторениями.
# Пусть у нас есть n предметов, из которых нужно выбрать k штук.
# Создайте функцию, зависящую от n и k, которая в зависимости от параметра with_repetitions будет подсчитывать количество сочетаний.
# Аргумент with_repetitions будет отвечать за вариант подсчёта: если он FALSE, то пусть считается количество сочетаний, а если TRUE, то сочетаний с повторениями.

# Пример:
# n=5 (Кол-во предметов)
# K=2 (нужно выбрать 2 предмета из 5ти)
# По умолчанию параметр with_repretitions = FALSE
# Результат: 
# Количество сочетаний без повторений: [1] 10
# Параметр with_repretitions = TRUE
# Количество сочетаний c повторениями: [1] 15


combin_count <- function(n, k, with_repretitions = FALSE) {
if (with_repretitions == FALSE) {return(factorial(n)/(factorial(k)*factorial(n-k)))}
else {return(factorial(k+n-1)/(factorial(n-1)*factorial(k)))}
}
