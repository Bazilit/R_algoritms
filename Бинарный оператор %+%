# Пусть %+% действует на два числовых вектора, складывая их поэлементно, но без учёта правил переписывания: если длина векторов различна, 
# то возвращаем вектор большей длины, но с пропущенными значениями в конце.

# Например,
# > 1:5 %+% 1:2
# [1]  2  4 NA NA NA

# > 5 %+% c(2, 6)
# [1]  7 NA


"%+%" <- function(x, y) {
  if (length(x) > length(y)) {
  n <- length(x)} else {
  n <- length(y)}
  x[1:n]+y[1:n]
}
